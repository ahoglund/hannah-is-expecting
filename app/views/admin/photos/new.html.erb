<%= render "photos_header" %>
<%= simple_form_for @photo, url: admin_photos_path do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :image, as: :file %>
  <%= f.association :albums, as: :check_boxes %>
  <%= f.button :submit, class: "btn btn-success" %>
<% end %>
